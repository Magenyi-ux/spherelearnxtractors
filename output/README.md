# Textbook Processing Pipeline Output Directory

All processed textbook files are stored here after execution.

Output formats:
- Structured JSON: `*_structured.json` (Optimized for AI retrieval, includes metadata, chapters, sections, semantic text chunks, formula objects, table objects, and image markers).
- Cleaned Text: `*_cleaned.txt` (Maintains heading hierarchy, resets spacing, and formats chapter streams cleanly).
